const fs = require('fs'); let content = fs.readFileSync('public/js/data.js', 'utf8'); content = content.replace(/precio_nacional":\s*(\d+),\s*"precio_extranjero":\s*(\d+)/g, (match, pn, pe) => { let pExt = Math.round(parseInt(pn) * 1.35 / 5) * 5; return 'precio_nacional": ' + pn + ',\n                "precio_extranjero": ' + pExt; }); fs.writeFileSync('public/js/data.js', content, 'utf8');
